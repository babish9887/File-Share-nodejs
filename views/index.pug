html(lang="en")
  head
    meta(charset="UTF-8")
    meta(http-equiv="X-UA-Compatible", content="IE=edge")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title File Share
    link(rel='stylesheet', href='/js/style.css')
  body(style="background: #39aea9")
    form(action="/upload", method="post", style="background: #fff; border-radius: 6px; padding: 20px; border: 2px solid #000; max-width: 350px; margin: 10% auto;", enctype="multipart/form-data")
      label(for="file") File:
      input(type="file", id="file", name="file", required, style="margin-bottom: 10px;")
      input(type="password", id="password", name="password", style="margin-bottom: 10px;")
      button(type="submit", style="width: 100%; padding: 10px; background-color: #39aea9; color: #fff; border: none; border-radius: 4px; cursor: pointer;") Share
    if fileLink != null
      div(style="margin-top: 1rem;")
        | Your file has been uploaded.
        a(href=fileLink, style="text-decoration: underline;")=fileLink
